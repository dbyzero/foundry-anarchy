{{!-- 
  data structure
  {
    code: 'wounds'
    flags:{
      optional,
      editable,
      displayForced,
    }
    label: 'Wounds modifier', // the label for the modifier
    value: 1,
    used: true,
    choices: [{value:choice1, label: label1},...]
    selected: choice1
  }
 --}}
 <label class="parameter-label">
  {{> 'systems/anarchy/templates/common/label.hbs' this}}
</label>
<label class="parameter-value">
  TODO
</label>

<label class="parameter-value">
{{#if flags.optional}}
  <input class="item-field-value flex-shrink check-optional {{#if isAnarchy}}anarchy-parameter{{/if}}" data-parameter-code="{{type}}" type="checkbox" {{checked used}} />
{{/if}}
No template defined for {{code}}
</label>

 {{!-- {{#if code}}
  <li class="flexrow" data-modifier="{{code}}">
    <label class="flex-grow-2" >{{label}}</label>
    {{log 'RollDialog.parameter ' parameter}}

    <span class="flex-shrink">
    {{#if optional}}
    <input class="item-field-value flex-shrink check-use-modifier {{#if isAnarchy}}anarchy-modifier{{/if}}" data-modifier="{{code}}" type="checkbox" {{checked used}} />
    {{/if}}

    {{#if (isInteger value)}}
      {{#if editable}}
        {{#if options}}
          <select name="select.range" class="select-option-modifier" data-dtype="String"
            data-modifier="{{code}}">
          {{#select value}}
          {{> 'systems/anarchy/templates/common/enum-value-label.hbs' entries=options noEmpty=true}}
          {{/select}}
          </select>
          <label class="selected-option-modifier-value">{{numberFormat value decimals=0 sign=false}}</label>
        {{else}}
          <input class="type-numeric input-select-modifier"
            data-modifier="{{code}}"
            type="number" data-dtype="Number" maxlength="2"
            value="{{either value 0}}"/>
        {{/if}}
      {{else}}
        <label>{{numberFormat value decimals=0 sign=true}}</label>
      {{/if}}
    {{/if}}
    </span>
  </li>
{{/if}} --}}
