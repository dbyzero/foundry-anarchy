<div class="flexrow item style-gear" data-item-id="{{_id}}" data-item-type="gear">
  {{#if img}}
  <img class="sheet-item-img" src="{{img}}" alt="name"/>
  {{/if}}
  <span>
    <label>{{name}}</label>
  </span>
  {{> 'systems/shadowrun-anarchy/templates/common/item-controls.hbs'}}
</div>
