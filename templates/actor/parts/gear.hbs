<div class="flexrow item anarchy-gear {{#if data.inactive}}item-inactive{{/if}}" data-item-id="{{_id}}" data-item-type="gear" draggable="true">
  {{#if img}}
  <img class="anarchy-img item-img" src="{{img}}" alt="{{name}}" title="{{name}}"/>
  {{/if}}
  <span>
    <label>{{name}}</label>
  </span>
  {{> 'systems/anarchy/templates/common/item-controls.hbs'}}
</div>
